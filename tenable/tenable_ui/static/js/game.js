$('#guess-form').submit(function(event) {
    let input = $('#guess');
    if (input.val().trim() === '') {
        event.preventDefault();
    }
});
$("game-over").click(function() {
    e.preventDefault()
    $
})